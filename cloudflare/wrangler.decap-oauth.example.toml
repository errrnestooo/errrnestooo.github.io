name = "ernest-journal-cms-auth"
main = "decap-oauth-worker.js"
compatibility_date = "2026-05-17"

# Recommended: add cms-auth.songyangao.com as a Worker custom domain in the
# Cloudflare account settings, or adapt this route to your zone setup.
# route = { pattern = "cms-auth.songyangao.com", custom_domain = true }

[vars]
ALLOWED_DOMAINS = "songyangao.com"
GITHUB_ALLOWED_LOGIN = "errrnestooo"
GITHUB_REPO_PRIVATE = "false"
